[build]
  command = "pnpm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Configure functions
[functions]
  node_bundler = "esbuild"
  included_files = ["drizzle/**"]

